import { ParseStartOfDayPipe } from './parse-start-of-day.pipe';

describe('ParseStartOfDayPipe', () => {
  it('should be defined', () => {
    expect(new ParseStartOfDayPipe()).toBeDefined();
  });
});
